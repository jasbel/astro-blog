---
import MainLayout from "../layouts/MainLayout.astro";
import BlogCard from "../components/blog/BlogCard.astro";
import { siteConfig } from "../config/site-config";
import { getCollection } from "astro:content";
import TypeBlogPost from "src/components/blog/TypeBlogPost.astro";

// const posts = await Astro.glob("./posts-old/*.{md,mdx}");
const blogPosts = await getCollection("blog", (p) => !p.data.isDraft);
console.log("🚀 ~ blogPosts", blogPosts);
---

<MainLayout>
  <h1 class="text-5xl text-white">Home</h1>
  <!-- component -->
  <section class="bg-white dark:bg-gray-900">
    <div class="container px-6 py-10 mx-auto">
      <h1 class="text-3xl font-semibold text-gray-800 capitalize lg:text-4xl dark:text-white">{siteConfig.title}</h1>

      <a href="/blogs/1">Blog paginado</a>

      <div class="grid grid-cols-1 gap-8 mt-8 md:mt-16 md:grid-cols-2">
        {blogPosts.map(d => <TypeBlogPost post={d} />)}
      </div>
      <!-- <div class="grid grid-cols-1 gap-8 mt-8 md:mt-16 md:grid-cols-2">
        {posts.map((d: any) => <BlogCard {...d} />)}
      </div> -->
    </div>
  </section>
</MainLayout>
<style>
  a {
    @apply text-blue-500 hover:underline
  }
</style>
